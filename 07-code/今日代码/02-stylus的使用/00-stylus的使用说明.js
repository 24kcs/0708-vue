/**
 * 
 * stylus的使用:
 * 1. npm install stylus -g  全局安装
 * 2. vscode中使用stylus的说明:
 *   css目录同级别下创建一个stylus的文件,后缀名.styl
 *  命令中:
 *  stylus -w index.styl -o css 
 *  在css目录中会自动的产生一个index.css的文件,在html中引入该css文件即可
 * 
 * 
 * 
 * 
 * 
 */
  